import { useEffect } from "react";

export default function TransitionController() {
  useEffect(() => {
    // Hero → About
    // About → Projects
    // Projects → Contact
    // Camera transitions
    // World transitions

  }, []);

  return null;
}